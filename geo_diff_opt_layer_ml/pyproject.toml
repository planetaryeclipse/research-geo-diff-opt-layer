[project]
name = "geo_diff_opt_layer_ml"
version = "0.1.0"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
# root folder specification to be usable in jupyter notebooks
where = [".."]
include = ["geo_diff_opt_layer_ml*"]